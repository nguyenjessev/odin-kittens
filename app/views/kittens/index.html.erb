<h1>Kittens#index</h1>
<p>Find me in app/views/kittens/index.html.erb</p>

<% @kittens.each do |kitten| %>
  <li>
    <%= link_to kitten.name, kitten %> (<%= link_to "Delete Kitten", kitten, method: :delete, data: { confirm: "Are you sure?" } %>)
  </li>
<% end %>

<div>
  <%= link_to "Create Kitten", new_kitten_path %>
</div>
